# 安装指南（从零开始）

面向第一次拿到这个项目的人。假设你的电脑上什么都没装。

---

## 先检查：你可能已经装了一部分

**双击 `0-检查环境.bat`**，它会一次性告诉你缺什么：

```
[1/6] Python  (need 3.9+, recommend 3.12)
      Python 3.12.9  -> OK
[2/6] Node.js
      v24.14.0  -> OK
[3/6] MySQL service
      Service MySQL80 NOT FOUND
      -> Install MySQL 8.0
[4/6] Config .env
      NOT FOUND
      -> Copy .env.example to .env
...
```

**显示 `OK` 的直接跳过**，只做 `NOT FOUND` 那几步。

> 电脑上已有旧版 Python 不用卸载 —— 多个版本可以共存，装在不同文件夹里互不影响。

---

## 需要准备什么

| 东西 | 用途 | 花多久 |
|---|---|---|
| Python 3.12 | 跑后端 | 5 分钟 |
| MySQL 8.0 | 存视频、评论、弹幕 | 20 分钟 |
| Node.js 18+ | 跑前端 | 3 分钟 |
| 硅基流动 API Key | 调用 AI 模型 | 3 分钟（免费注册） |
| B 站账号 Cookie | 抓评论用（选填） | 2 分钟 |

---

## 第 1 步 · 装 Python 3.12

> 检查脚本 [1/6] 显示 OK 就跳过。

下载：https://www.python.org/downloads/release/python-3129/
往下拉，点 **Windows installer (64-bit)**。

安装时**第一屏最下面那个复选框必须勾上**：

```
☑ Add python.exe to PATH        ← 默认不勾，一定要手动勾
```

不勾的话后面所有命令都会报"找不到 python"，或者弹出微软商店。

验证：

```powershell
python --version      # 应输出 Python 3.12.x
```

> **为什么要 3.9 以上**：项目依赖的 langchain 要求 Python ≥ 3.9。
> 用 3.8 装依赖会失败，报错是「找不到满足要求的版本」，
> 不会告诉你真正原因是 Python 太老。

---

## 第 2 步 · 装 MySQL 8.0

> 检查脚本 [3/6] 显示 `RUNNING -> OK` 就跳过。
> 显示「Installed but NOT RUNNING」的话只需启动服务，不用重装。

下载：https://dev.mysql.com/downloads/installer/
选大的那个 `mysql-installer-community-8.0.xx.msi`（约 300MB，不是 2MB 的 web 版）。
让你登录 Oracle 账号时点下方小字 **"No thanks, just start my download"** 跳过。

安装时的关键选择：

| 步骤 | 选什么 |
|---|---|
| Setup Type | **Custom**，只勾 `MySQL Server 8.0` + `MySQL Workbench` |
| Type and Networking | Development Computer，端口 **3306** 保持默认 |
| Authentication Method | 保持默认（Strong Password Encryption） |
| **Accounts and Roles** | **设置 root 密码，记下来**，第 5 步要填 |
| Windows Service | 保持默认，勾选开机自启 |

最后 **Apply Configuration → Execute**，等所有项变绿勾。

> MySQL Workbench 别漏勾，它是图形化查看数据的工具。

---

## 第 3 步 · 装 Node.js

> 检查脚本 [2/6] 显示 OK 就跳过。

下载：https://nodejs.org/ ，选 LTS 版本，一路 Next。

---

## 第 4 步 · 拿代码

```powershell
cd D:\                                              # 决定放在哪
git clone https://github.com/你的用户名/BiliAgent.git
cd BiliAgent
```

没装 git 的话，也可以在仓库页面点 **Code → Download ZIP**，解压即可。

---

## 第 5 步 · 配置（最关键的一步）

### 5.1 拿硅基流动 API Key

1. 打开 https://cloud.siliconflow.cn/
2. 手机号注册登录
3. 左侧 **API 密钥** → **新建 API 密钥**
4. 复制 `sk-` 开头的那串

新用户有赠送额度，够跑很久。

### 5.2 拿 B 站 Cookie（选填，但建议做）

**不填也能搜索视频、抓弹幕，但拿不到评论**——B 站评论接口需要登录态。

1. 浏览器登录 https://www.bilibili.com
2. 按 **F12** 打开开发者工具
3. 切到 **Application / 应用** 标签
4. 左侧展开 **Cookies** → `https://www.bilibili.com`
5. 在右侧找到这四项，复制它们的 Value：

```
SESSDATA      →  填到 sessdata
bili_jct      →  填到 bili_jct
buvid3        →  填到 buvid3
DedeUserID    →  填到 dedeuserid
```

> ⚠️ **这几个值等同于你的 B 站登录状态**，拿到的人能以你的身份操作账号。
> 绝对不要提交到 GitHub、不要发给别人。不慎泄露的话，去 B 站「退出所有设备登录」即可作废。

### 5.3 创建两个 .env 文件

**根目录**：把 `.env.example` 复制一份改名成 `.env`，用记事本打开，填三处：

```
SILICON_API_KEY=在这里填你的Key       →  换成 5.1 拿到的
MYSQL_PASSWORD=你的MySQL密码          →  换成第 2 步设的
sessdata= / bili_jct= / ...           →  换成 5.2 拿到的（不填也能跑）
```

**前端目录**：把 `frontend\.env.example` 复制成 `frontend\.env`，通常不用改。

> `=` 两边不要加空格，值不要加引号。
> 这两个 `.env` 都不会上传到 GitHub（`.gitignore` 挡着）。

---

## 第 6 步 · 装依赖

> 检查脚本 [5/6] [6/6] 都显示 OK 就跳过。

### 后端

```powershell
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

国内网络慢的话加镜像：

```powershell
.venv\Scripts\python.exe -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 前端

```powershell
cd frontend
npm install
```

慢的话用：

```powershell
npm install --registry=https://registry.npmmirror.com
```

> 这一步会下载约 600 MB 的第三方库，装进 `.venv\` 和 `frontend\node_modules\`。
> 这两个文件夹不进版本库，每台电脑都要自己装一次。

---

## 第 7 步 · 建表

```powershell
.venv\Scripts\python.exe init_db.py --init
```

会自动创建 16 张表。看到「数据库表创建成功」就好了。

> 数据库本身需要先存在。如果报错说找不到库，先在 MySQL Workbench 里执行：
> ```sql
> CREATE DATABASE bilibili_analysis DEFAULT CHARACTER SET utf8mb4;
> ```

---

## 第 8 步 · 启动

回到项目根目录，**双击两个 bat**：

```
1-启动后端.bat      等它出现 Uvicorn running on http://127.0.0.1:8300
2-启动前端.bat      等它出现 Local: http://127.0.0.1:5173/
```

两个黑窗口留着别关，浏览器打开：

```
http://127.0.0.1:5173
```

第一次要先**注册一个账号**，然后登录。

---

## 第 9 步 · 跑通一次完整流程

```
① 「爬虫管理」页  →  输入关键词（比如"旅游攻略"）→ 开始抓取
                     后端窗口会实时打印抓取日志
② 「数据集」页    →  看到刚生成的数据集，记下它
③ 「课题」页      →  新建课题 → 关联刚才的数据集
④ 「智能问答」页  →  选中数据集 → 提问，答案会带视频链接
⑤ 「课题报告」页  →  生成报告，看词云、情感分布、时间趋势
```

**第一次抓取建议只用一个关键词、少量视频**，先确认链路通了再加量。

---

## 常见问题

### `python` 命令弹出微软商店

第 1 步没勾 "Add python.exe to PATH"。重新运行安装程序，选 Modify，勾上它。

### 装依赖报「找不到满足要求的版本」

Python 版本太老。执行 `python --version` 确认是 3.9 以上。

### 后端报「无法连接 MySQL」

- 服务没启动：`Win+R` → `services.msc` → 找 **MySQL80** → 右键启动
- 密码不对：检查 `.env` 里的 `MYSQL_PASSWORD`
- 库不存在：执行第 7 步的 `CREATE DATABASE`

### 抓取时评论数总是 0

`.env` 里的 B 站 Cookie 没填或已失效。重新按 5.2 拿一遍。
（Cookie 有有效期，过期了要重新获取。）

### 提问时报「API 调用失败」

`.env` 里的 `SILICON_API_KEY` 没填、填错，或者额度用完了。

### 报错 10048「端口只允许使用一次」

已经有服务占着 8300 或 5173 了。

后端有**自动换端口**的逻辑，会依次尝试 8301、8302…最多 10 次。
但那样前端就连不上了（前端写死了 8300），需要同步改 `frontend\.env`。

建议直接把旧窗口关掉再启动。查是谁占着：

```powershell
Get-NetTCPConnection -LocalPort 8300 -State Listen | Select-Object OwningProcess
```

### 网页能打开但数据加载不出来

前端起来了、后端没起来，或者两边端口对不上。
检查后端窗口有没有报错，以及 `frontend\.env` 里的地址是否匹配。

---

## Mac / Linux 的差异

| | Windows | Mac / Linux |
|---|---|---|
| 建虚拟环境 | `python -m venv .venv` | `python3 -m venv .venv` |
| 虚拟环境的 python | `.venv\Scripts\python.exe` | `.venv/bin/python` |
| 启动脚本 | 双击 `.bat` | 手敲命令 |

```bash
# 终端 1
.venv/bin/python server_extended.py

# 终端 2
cd frontend && npm run dev
```

MySQL 用 Homebrew 装更省事：`brew install mysql && brew services start mysql`

> ⚠️ `blibli_get/` 目录下有三个中文文件名的 Python 模块。
> 在部分 Mac/Linux 环境下（locale 设置不当时）可能出现导入问题。
> 遇到的话把它们改成英文名，并同步修改 `pipeline.py` 顶部的 import 语句。

---

## 装好之后

- 项目是什么、架构如何 → [README.md](README.md)
- 日常启动、命令速查 → [启动说明.md](启动说明.md)
