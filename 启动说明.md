# 怎么启动这个项目

> 忘了怎么用的时候先看这里。第一次配环境请看 [安装指南.md](安装指南.md)。

---

## 最简单：双击两个 bat

在项目根目录：

```
1-启动后端.bat      等它出现 "Uvicorn running on http://127.0.0.1:8300"
2-启动前端.bat      等它出现 "Local: http://127.0.0.1:5173/"
```

浏览器打开：

```
http://127.0.0.1:5173
```

两个黑窗口都要留着别关。不用了直接关窗口就行。

---

## 一共有两个网页

| 地址 | 是什么 | 什么时候用 | 需要启动 |
|---|---|---|---|
| **http://127.0.0.1:5173** | **正式界面** | **日常使用** | 后端 + 前端 |
| http://127.0.0.1:8300/docs | 接口测试台 | 调试、直接戳后端 | 只要后端 |

`8300/docs` 是 FastAPI 自动生成的，能看到全部接口并直接调用，做前端之前用它验证后端。

---

## 三个端口分别是谁

```
127.0.0.1:3306    MySQL 数据库     开机自启，平时不用管
127.0.0.1:8300    后端 (Python)    1-启动后端.bat
127.0.0.1:5173    前端 (Node)      2-启动前端.bat
```

三个都要在跑，链条才完整：

```
浏览器 → 5173 前端 → 8300 后端 → 3306 MySQL
```

---

## 手动启动（不用 bat 的话）

需要两个 PowerShell 窗口。

### 窗口 1 · 后端

```powershell
cd D:\BiliAgent-main-1.11
.venv\Scripts\python.exe server_extended.py
```

后端支持这些参数：

```powershell
.venv\Scripts\python.exe server_extended.py --port 8300 --reload
```

| 参数 | 作用 |
|---|---|
| `--port 8300` | 指定端口（默认读 `.env` 的 `SERVER_PORT`） |
| `--reload` | 改代码自动重启，开发时好用 |
| `--host 0.0.0.0` | 允许局域网内其他设备访问 |
| `--no-auto-port` | 端口被占时直接报错，不自动换 |

> 默认开启了「端口被占自动递增」——8300 不通就试 8301、8302…
> 这时候前端会连不上（它写死了 8300），需要同步改 `frontend\.env`。

### 窗口 2 · 前端

```powershell
cd D:\BiliAgent-main-1.11\frontend
npm run dev
```

---

## 完整使用流程

```
① 注册 / 登录
② 「爬虫管理」→ 输入关键词 → 开始抓取
     后端窗口会实时打印 [video] [comments] [danmaku] 日志
③ 「数据集」  → 查看抓到的数据、导出 jsonl/csv
④ 「课题」    → 新建课题 → 关联数据集
⑤ 「智能问答」→ 选数据集 → 提问，答案带视频链接，逐字输出
⑥ 「课题报告」→ 生成词云、情感分布、时间趋势、LLM 总结
```

---

## 常用命令

```powershell
cd D:\BiliAgent-main-1.11

# 建表（首次或加了新表时）
.venv\Scripts\python.exe init_db.py --init

# 删表重来（危险，会清空所有数据）
.venv\Scripts\python.exe init_db.py --drop

# 生成工作流图（需要 graphviz）
.venv\Scripts\python.exe visualize_graph.py
```

---

## 常用 SQL

在 MySQL Workbench 里执行：

```sql
USE bilibili_analysis;

-- 抓了多少数据
SELECT
  (SELECT COUNT(*) FROM video_item)    AS 视频数,
  (SELECT COUNT(*) FROM video_comment) AS 评论数,
  (SELECT COUNT(*) FROM video_danmaku) AS 弹幕数;

-- 各数据集的规模
SELECT dataset_id, name, keyword, video_count, comment_count, danmaku_count
FROM dataset ORDER BY id DESC;

-- 抓取任务的执行情况
SELECT task_id, keyword, status, video_count, comment_count, create_time
FROM crawl_task ORDER BY id DESC LIMIT 10;

-- 最近的问答记录
SELECT role, LEFT(content, 60) AS 内容, create_time
FROM message_history ORDER BY id DESC LIMIT 10;

-- 某个视频的评论
SELECT c.content FROM video_comment c
JOIN video_item v ON v.id = c.video_id
WHERE v.title LIKE '%关键词%' LIMIT 20;
```

---

## 爬虫限流参数

在 `.env` 里加这几行可以覆盖默认值：

```
BILI_CRAWLER_MAX_ITEMS=5         每个关键词抓几个视频
BILI_CRAWLER_MAX_COMMENTS=10     每个视频抓几条评论
BILI_CRAWLER_MAX_COMMENT_PAGES=1 评论翻几页
BILI_CRAWLER_MAX_REPLIES=3       每条评论抓几条二级回复
BILI_CRAWLER_MAX_DANMAKU=50      每个视频抓几条弹幕
BILI_CRAWLER_CONCURRENCY=5       并发线程数
```

**默认值定得很保守，是有意的。** 加大之前请考虑对方服务器的承受能力，
也考虑自己的账号安全——频率过高可能触发风控。

---

## 常见问题

### 网页打不开 / 一直转圈

先看两个黑窗口还在不在、有没有报错。多半是其中一个没起来。

### 页面能开但数据加载不出来

前端起来了、后端没起来。检查后端窗口。

也可能是端口对不上：后端自动换到了 8301，而 `frontend\.env` 还写着 8300。

### 后端报连不上 MySQL

`Win+R` → `services.msc` → 找 **MySQL80** → 右键启动。

### 抓取时评论数一直是 0

`.env` 里的 B 站 Cookie 失效了。Cookie 有有效期，重新按
[安装指南](安装指南.md) 第 5.2 节获取一次。

### 提问报「API 调用失败」

`.env` 里的 `SILICON_API_KEY` 失效或额度用完。
去 https://cloud.siliconflow.cn/ 查看。

### 端口被占用（10048）

```powershell
Get-NetTCPConnection -LocalPort 8300 -State Listen | Select-Object OwningProcess
```

查到 PID 后在任务管理器里结束它，或者直接关掉旧的黑窗口。

### 改了代码不生效

**Python 在启动那一刻把代码读进内存，改文件它不知道。**
必须重启后端。启动时加 `--reload` 可以自动重启。

确认真的重启了：关掉服务后浏览器打开 `http://127.0.0.1:8300/docs`，
**应该打不开**。打不开才算真关了。

---

## 向量索引

```
vectorstores/ds_<md5>/
├── index.faiss    向量矩阵
└── index.pkl      原文 + 映射
```

目录名是「所选数据集 ID 组合」的 md5。选不同的数据集组合会生成不同的索引。

**索引可以删掉重建** —— 删除 `vectorstores/` 下的目录后，
下次提问时会自动从 MySQL 重新拉数据、重新调用嵌入 API 生成。

⚠️ 重建会消耗 API 额度，数据量大时要等一会儿。
